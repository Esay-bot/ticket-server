# UI 还原度走查与美化 Agent 开发计划（带做模式）

> **给新对话的使用方法**：在 `D:\桌面\my\ser-cli` 目录下打开 AI 编程助手，发送：
> 「请完整阅读 UI美化Agent开发计划.md，从 M0 开始，按"讲解 → 我动手 → 反馈 → 验收"的节奏带我做完，**不要一次生成全部代码**，核心代码由我自己写，你负责讲解、给骨架和验收。」

---

## 一、背景与目标

- **我是谁**：王思雨，大四，有 Qt 实习（真实做过：拿 UI 设计稿指挥 AI 批量改界面——图标、布局、QSS 样式）。
- **要做什么**：把实习里"人肉驱动 AI 改界面"的流程做成一个**可运行的走查与美化 Agent**——输入「设计稿截图 + 当前界面截图」，Agent 用视觉模型对比差异，生成 / 迭代 QSS 修改，输出走查报告。
- **与简历的关系**：
  1. 支撑实习经历里"自建界面美化 Agent 式工作流"那条——面试时说"实习里是人工拆解 + AI 执行，后来我把这套流程做成了工具"，有仓库、有录屏可展示；
  2. 做扎实后，用它**替换 AI 简历里的企业制度知识库项目**，让两个项目都是自己做的（见第八节预写条目）。
- **工期**：3～4 天（每天 4～6 小时）。
- **运行环境**：**Windows 直接可做**（不需要 Linux）——被改造的界面是 Qt 程序，视觉模型走 API。

## 二、教学规则（AI 助手必须遵守）

1. 每个模块按 **讲解 → 我动手 → 反馈 → 验收** 推进。
2. **核心代码（视觉对比解析、Agent 循环、报告生成）由我亲手写**，AI 只给骨架与提示。
3. 卡住先给思路，尝试 2 次不行再给答案。
4. 验收全过才进下一模块。
5. 每天结束复述：输入、输出、状态变化、失败分支。

## 三、技术方案（已定）

- **语言/形态**：Python 命令行工具（先 CLI，不做 Web 界面）。
- **视觉模型**：GLM-4V-Flash（智谱，免费、OpenAI 兼容接口）；备选 Qwen-VL。API Key 放环境变量 `ZHIPU_API_KEY`。
- **被改造对象**：自己的 Qt 程序——首选正在做的票务客户端（Qt 计划做到 M4 即可用）；若还没动工，先用一个 30 分钟搭的最小 demo（登录框 + 表格 + 几个按钮）当靶子，**不要等**。
- **设计稿**：自己做 2～3 套目标风格图（改配色、换图标、调布局；用 PPT / 画图 / Figma 都行，截图保存即可）。
- **截图**：Windows 上用 Pillow `ImageGrab` 截指定窗口区域。
- **改造方式**：Agent 只产出 / 修改 `.qss` 文件（样式层面），**不改布局代码**——这是能力边界，报告里对布局类差异只标注不建议自动改。
- 目录：`D:\桌面\my\ser-cli\ui-agent\`。

## 四、模块划分与验收

### M0 准备（半天）
- 建好 Qt 靶子程序（或票务客户端 M0-M3 成果），带 QSS 文件加载；准备 2～3 套目标风格图。
- 调通视觉模型：发两张图 + "列出界面差异"，拿到一次原始返回。
- **验收**：靶子程序能加载 QSS 启动；API 调用成功返回。

### M1 视觉对比基线（核心，我亲写）
- `compare(mockup_path, shot_path) -> 差异清单`：提示词强制要求**结构化 JSON 输出**（差异项：类型[颜色/图标/间距/圆角/布局]、位置描述、建议修改值），并做解析校验与重试（模型输出不合 schema 时最多重试 2 次）。
- 工具函数：`grab_window()`（截靶子窗口）、`crop_region(x,y,w,h)`（局部放大复核，降低幻觉）。
- **验收**：对一对差异明显的图，输出可解析的 JSON 清单；对完全相同的图，输出空清单。

### M2 QSS 读写与安全应用
- `read_qss()` / `write_qss()`：修改前自动备份（带时间戳），QSS 语法错误时不落盘；靶子程序支持运行中重载 QSS（快捷键或文件监听）。
- 图标检查：`list_icons()` 对比资源目录与设计稿中图标清单。
- **验收**：改坏能一键回滚；语法错误被拦截。

### M3 Agent 循环（核心，我亲写）
- 编排：对比 → 逐项生成 QSS 修改 → 应用 → 重新截图 → 复检，循环直到差异条数 ≤ 阈值或不再下降或达最大轮数（默认 5）。
- 每轮记录：差异条数变化、修改内容、token 消耗。
- **验收**：对一个"旧→新"任务，3～5 轮内差异条数明显下降且不反弹；中途 Ctrl+C 或出错不丢已完成的轮次记录。

### M4 走查报告
- 输出 `report.html`（或 Markdown）：前后对比图、每轮差异清单、QSS 变更 diff、轮次 / token / 耗时统计、未解决的布局类差异标注"需人工"。
- **验收**：一个不看代码的人能看懂这份报告。

### M5 评测（半天）
- 10 个界面任务（不同风格目标图 × 不同起始界面），记录：差异项收敛率、平均轮数、token 成本、人工还原度评分（1～5，自己评 + 找同学评）。
- **验收**：真实数据表跑出来；至少分析 2 个不收敛的失败案例。

### M6 收尾（半天）
- README（架构、用法、边界：人机协作定位说明）、3 分钟演示录屏（走查 + 一轮自动美化 + 报告）。
- 完成第九节自测题；若要上简历，用第八节条目替换知识库项目（找我改简历）。

## 五、能力边界（面试就这么答）

这是**人机协作的半自动工具**，不是全自动界面生成：模型负责对比与样式建议，QSS 应用有备份回滚，布局级改动只标注不改。这个定位是刻意取舍——全自动改布局风险不可控。诚实说"半自动"比吹"全自动"可信得多。

## 六、时间参考

| 天 | 内容 |
|---|---|
| 第 1 天 | M0 + M1（视觉对比是地基） |
| 第 2 天 | M2 + M3（循环是灵魂） |
| 第 3 天 | M4 + M5（报告与评测出数据） |
| 第 4 天 | M6（收尾 + 自测 + 可选换简历） |

落后时优先砍：图标检查、HTML 报告（降级为 Markdown）；**不砍 M1 结构化对比、M3 循环、M5 评测**。

## 七、与其他计划的关系

- **票务 Agent（方案 A）优先级更高**：它支撑 AI 简历第一项目，先做完它；本计划排在其后。
- 本项目与 **Qt 客户端计划**共用界面：Qt 客户端先做出骨架，正好给本项目当靶子，一举两得。

## 八、简历预写条目（完成后填真实数据，用于替换知识库项目）

> UI 还原度走查与美化 Agent：基于视觉大模型（GLM-4V）与工具调用循环，自动对比 UI 设计稿与实现截图，输出结构化差异清单并迭代生成 QSS 修改，支持"截图 → 对比 → 修改 → 复检"的收敛闭环；在自研 Qt 客户端上完成 10 项界面任务评测，差异项平均收敛 __%，支持 QSS 备份回滚与走查报告生成。

## 九、面试自测题（M6 结束全答）

1. 视觉模型输出不稳定（同一对图两次结果不同），你做了哪些工程约束？（schema 强制、低温、重试、crop 复核）
2. 怎么防止 Agent 把界面改坏？回滚机制怎么设计的？
3. 收敛判定用什么指标？为什么不追求差异清零？
4. 为什么只做 QSS 不动布局代码？这个边界怎么权衡的？
5. 这和实习里的流程是什么关系？（人工流程 → 工具化，讲清演进）
6. 它是 Agent 还是固定流程？（对比→改→复检是循环 + 模型决策，接近 Agent；如实分析）
7. token 成本怎么控制和估算的？
8. 如果要把差异定位从"区域描述"升级为"精确控件定位"，你会怎么做？（控件树导出 / accessibility 信息——说出思路即可）
