# Qt 客户端开发计划 —— 票务预约系统（带做模式）

> **给新对话的使用方法**：在 `D:\桌面\my\ser-cli` 目录下打开 AI 编程助手（工作目录必须是本项目），发送：
> 「请完整阅读 Qt客户端开发计划.md，从 M0 开始，按"讲解 → 我动手 → 反馈 → 验收"的节奏带我做完，**不要一次生成全部代码**，核心代码由我自己写，你负责讲解、给骨架和验收。」

---

## 一、背景与目标

- **我是谁**：王思雨，大四，C++/Linux 后端基础扎实，有一段 Qt 实习（弹窗开发、QSS、公共模块重构），做过本项目的命令行客户端。
- **要做什么**：把 `client/` 下的命令行客户端升级为 **Qt 图形客户端**，替换掉命令行版本。
- **工期**：2～4 天（每天 4～6 小时）。
- **硬性要求**：做完后简历上已预写的条目必须逐字属实（见第六节），且我能独立讲解每一行核心代码、能现场加需求。

## 二、教学规则（AI 助手必须遵守）

1. 每个模块按 **讲解 → 我动手 → 反馈 → 验收** 推进；讲解包含：目标、涉及知识点、设计取舍。
2. **核心代码（协议编解码、网络层封装、Model/View）由我亲手写**，AI 只给类骨架、函数签名和提示，不给可直接复制的完整实现。
3. 卡住时先给思路和调试方法（断点位置、日志打法），我尝试 2 次仍不行再给答案。
4. 每个模块的验收标准全部通过才能进入下一模块。
5. 每天结束时让我复述：当天模块的输入、输出、状态变化、失败分支。

## 三、现有服务端事实（以代码为准，不要臆测）

代码位置：`D:\桌面\my\ser-cli`（`server/` 服务端，`client/` 旧命令行客户端，`qt-client/` 本次新建）。

- **网络**：服务端监听 `127.0.0.1:6000`（`server/ser.cpp`）。
- **架构**：libevent 单线程 IO 循环 + 4 线程线程池；业务在工作线程执行，响应经匿名管道回到 IO 线程发送。**服务端是"一问一答"模式，响应不回显请求 type，客户端必须串行发送请求（同一时刻只允许一个未完成请求）。**
- **应用层协议**：`4 字节大端（网络序）长度头 + JSON 文本`。JSON 由 jsoncpp `toStyledString()` 生成（带缩进换行），长度为字节数。
- **操作枚举**（JSON 的 `type` 字段）：`Login=1, Register=2, View=3, Reserve=4, MyReserve=5, Cancel=6`。

### 请求 / 响应字段明细

| 操作 | 请求 | 成功响应 | 失败响应 |
|---|---|---|---|
| 登录 | `{type:1, user_tel, user_passwd}` | `{status:"OK", user_name}` | `{status:"ERR"}` |
| 注册 | `{type:2, user_tel, user_name, user_passwd}` | `{status:"OK"}` | `{status:"ERR"}` |
| 查票 | `{type:3}` | `{status:"OK", num, arr:[{tk_id,addr,max,num,use_date}]}` | `{status:"ERR"}` |
| 预约 | `{type:4, tel, index}`（index=票的 tk_id） | `{status:"OK"}` | `{status:"ERR"}` |
| 我的预约 | `{type:5, tel}` | `{status:"OK", num, arr:[{yd_id,addr,use_date}]}` | `{status:"ERR"}` |
| 取消预约 | `{type:6, tel, index}`（index=预约的 yd_id） | `{status:"OK"}` | `{status:"ERR"}` |

- **数据库**：MySQL 本地库 `Project_DB`（表 `user_info` / `ticket_info` / `reserve_ticket`），连接参数见 `server/ser.cpp`，仅本地开发用。
- **服务端编译参考**（无现成构建脚本，M0 时验证）：
  `g++ -std=c++11 server/*.cpp -levent -ljsoncpp -lmysqlclient -o ser`
- 响应超时：旧客户端设了 5 秒 `SO_RCVTIMEO`，Qt 客户端用 `QTimer` 实现同等效果。

## 四、技术选型（已定，不再讨论）

- Qt 5.15 或 Qt 6（以实习环境/本机已装版本为准），Qt Creator 或 VS 均可。
- JSON：用 **QJsonDocument / QJsonObject**（不引 jsoncpp，更 Qt 化、少依赖）。
- 大端长度头：`qToBigEndian` 或手动移位，**不用 QDataStream 序列化结构体**。
- 新建目录 `qt-client/`，CMake 或 qmake 工程均可。
- **明确不做**：手写 QThread 收发线程（QTcpSocket 本身就是异步信号驱动）；多请求并发管线；聊天式界面。

## 五、模块划分与验收标准

### M0 环境与工程骨架（半天内）
- 确认 Qt 开发环境可用；新建 `qt-client` 工程，空窗口编译运行通过。
- 把服务端编译跑起来（顺手补一个 `build.sh` 或 Makefile + README 启动说明）。
- 用旧命令行客户端完整走一遍 登录→查票→预约→取消，确认服务端正常。
- **验收**：Qt 空窗口运行 ✔ + 命令行客户端业务闭环 ✔。

### M1 协议编解码（核心，我亲写）
- 类 `ProtocolCodec`：
  - `QByteArray encode(const QJsonObject &obj)`：4 字节大端长度 + JSON 文本。
  - `void feed(const QByteArray &bytes)`：追加到内部缓冲；够一个完整包就 emit `packetReady(QJsonObject)`；循环取包处理**粘包**，不够整包就等下次（**半包**）。
  - 长度非法（0 或 > 4096）要丢弃缓冲并报错，不能死循环。
- 知识点：大端序、`QJsonDocument` 序列化、和服务端 muduo 风格 Buffer 拆包思路的对应关系。
- **验收**：构造三组测试数据——半包分两次 feed、两个包粘在一起一次 feed、非法长度——结果全部正确。

### M2 网络层封装
- 类 `TcpClient`（封装 `QTcpSocket`）：
  - `connectToHost()` / `send(const QJsonObject&)`（内部 encode 后 write）。
  - `readyRead` → `codec.feed()`；`connected` / `disconnected` / `errorOccurred` / `QTimer` 5 秒超时。
  - 信号：`jsonReceived(QJsonObject)`、`connStateChanged(bool)`、`errorOccurred(QString)`。
  - **同一时刻只允许一个未完成请求**（发送后锁定，收到响应或超时后解锁），并能说清为什么（服务端响应不带 type 回显）。
- **验收**：临时界面或测试代码完成一次真实登录请求-响应往返，日志打印收发内容。

### M3 登录 / 注册界面
- 登录 `QDialog`：手机号 + 密码 + 登录/注册按钮（注册加用户名与二次密码确认）。
- 输入校验（空值、两次密码一致）、等待态（请求期间禁用按钮 + "连接中…"提示）、失败原因展示。
- **验收**：真实账号登录成功进入主界面；错误密码、重复注册均有明确提示；请求期间按钮不可连点。

### M4 主界面 · 车票列表（核心，我亲写）
- 自定义 `TicketTableModel : QAbstractTableModel`（列：编号 / 线路 / 总票数 / 已预约 / 日期），`QTableView` 展示，"刷新"按钮触发查票。
- 知识点：为什么用 Model/View 而不是 `QTableWidget`；`rowCount / columnCount / data / headerData` 与 `DisplayRole`；数据更新用何种方式刷新（`beginResetModel` 等）。
- **验收**：表格正确显示服务端返回数据，刷新按钮生效，无内存告警。

### M5 预约 / 取消 / 我的预约全流程 + 断线处理
- 选中行 → 预约；"我的预约"表格 → 选中取消；操作结果提示；成功后自动刷新相关表格。
- 断线处理：手动 kill 服务端 → 状态栏提示"已断线"、所有操作按钮禁用；提供"重连"按钮，重连成功后恢复界面。
- **验收**：完整业务闭环 ✔；断线时界面不卡死、不崩溃 ✔；重连后可继续操作 ✔。

### M6 收尾（半天）
- 统一 QSS（简洁即可，**不要**在这上面花超过 1 小时）。
- `qt-client/README.md`：架构说明（一张图）、编译启动方式、协议说明、已知限制。
- 3 分钟演示录屏：登录 → 查票 → 预约 → 我的预约 → 取消 → 断线重连。
- 对照第六节逐条核对简历描述；有出入**先改简历再投**。
- 完成第七节面试自测问题，全部能口头答出。

## 六、简历预写条目（完成后逐项核对）

> 使用 Qt 开发图形客户端：基于 QTcpSocket 信号槽实现异步收发，复用"4 字节长度头 + JSON"协议与缓冲区拆包逻辑，QTableView 呈现车票与预约信息，覆盖登录 / 查询 / 预约 / 取消全流程，并处理断线提示与重连。

核对清单：QTcpSocket 异步收发 ✔｜缓冲区拆包（M1）✔｜QTableView（M4）✔｜四类操作全流程（M3+M5）✔｜断线提示与重连（M5）✔。
**任何一项没做出来，先从简历删掉对应表述。**

## 七、面试自测问题（M6 结束必须全答）

1. 信号槽的实现原理？与函数指针回调相比的优缺点？Qt5 新式 connect 语法为什么类型安全？
2. `readyRead` 的触发时机？一次 `readyRead` 一定对应一个完整数据包吗？你如何处理？
3. 你客户端的粘包/半包处理和服务端 muduo 风格 Buffer 有什么异同？
4. 为什么用 `QTableView` + 自定义 Model 而不是 `QTableWidget`？数据变了如何通知视图刷新？
5. 用户连点两次"预约"会发生什么？你用什么机制防止重复请求？
6. 断线后如何让整窗进入不可用状态？重连成功后如何恢复？状态变量存在哪里？
7. `QJsonObject` 解析时字段缺失或类型不符怎么处理？你的代码里哪些地方做了防御？
8. 如果把 `QTcpSocket` 移到子线程使用，需要注意什么？（信号槽跨线程连接类型、线程事件循环、UI 只能在主线程改）——本项目没这么做，但必须能讲。
9. 这个 Qt 客户端和实习里公司项目的工程化差距有哪些？（构建系统、代码规范、评审流程、测试）

## 八、时间参考

| 天 | 内容 |
|---|---|
| 第 1 天 | M0 + M1（协议层是地基，时间花够） |
| 第 2 天 | M2 + M3 |
| 第 3 天 | M4 + M5 |
| 第 4 天 | M6 + 面试自测 + 更新简历核对 |

进度落后时优先砍：QSS 美化、重连按钮（保留断线提示），**不砍 M1 协议层和 M4 Model/View**——这两块是简历条目的技术含金量所在。
