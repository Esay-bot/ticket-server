# Agent 运行依赖(Python 3.10+)
# 安装: pip install -r requirements.txt
openai>=1.30.0
fastapi>=0.110.0      # V1-M1 服务化层
uvicorn>=0.27.0      # 服务层 ASGI 服务器
httpx>=0.27.0        # test_service 的 TestClient 依赖
